name: flutter_arch_risk
description: Deterministic architecture scale risk scanner for Flutter projects.
version: 0.1.0
repository: https://github.com/yourusername/flutter_arch_risk

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2
  yaml: ^3.1.2

dev_dependencies:
  test: ^1.24.0

executables:
  flutter_arch_risk:
