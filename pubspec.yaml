name: scale_guard
description: ScaleGuard — deterministic architecture scale risk scanner for Flutter projects.
version: 0.2.1
repository: https://github.com/PavelK2254/flutter_scaleguard

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2
  yaml: ^3.1.2

dev_dependencies:
  test: ^1.24.0

executables:
  scale_guard:
